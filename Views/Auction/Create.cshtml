<h3>New Auction!</h3> 
<a href = "/show"> Home </a> 
    <a asp-controller="User" asp-action="Logout"> Logout </a>
<form asp-controller="Auction" asp-action="Auctionform">
    <label for='ProductName'>ProductName: </label>
        <input type='ProductName' name='ProductName'>
    <label for='Description'>Description: </label>
        <input type='Description' name='Description'>
    <label for='EndDate'>EndDate: </label>
        <input type='date' name='EndDate'>
    <label for='StartingBid'>StartingBid: </label>
        <input type='StartingBid' name='StartingBid'>
    <button type='submit' class='btn btn-default'>Create Auction</button>
    
</form>
<div>
    @{
        if (@ViewBag.status=="Auctfail"){
        foreach(var error in ViewBag.errors)
            {

                if(@error.Errors.Count > 0)
                {
                    <p>@error.Errors[0].ErrorMessage</p>
                }
            }
    
        }
if (@ViewBag.status=="Auctiondatefailure"){
    @ViewBag.errors
}
        
    }